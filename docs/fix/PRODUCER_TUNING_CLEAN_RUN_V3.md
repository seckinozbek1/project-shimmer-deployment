# Producer tuning clean-run V3

Use: `curation_only`. Evidence: machine-agent label curation. No training authorization.

Versions: `producer-semantic-policy-v3`, `producer-coverage-amendment-v3`, `producer-target-renderer-v3`. Starting HEAD: `6ff6f0792646ef37ea562e396be0aa6b7d7a9818`.

## Prior V2 failure
V2's historical target-reading regression ran before its label-freeze commit. Its otherwise-qualified labels remain quarantined and immutable. This run creates new policy, amendment and renderer identifiers ending in v3.

## Chronology architecture
Method freeze: `749c82c9595ec57f5217726df659bf89940b5873`.

Phases 0?4 are target-free: generic design, committed method freeze, packet-only review, consensus/adjudication/rendering, committed label freeze. Phases 5?6 permit authored comparison and candidate calculation only after a full commit-object reference, exact committed manifest, unchanged hashes, active V3 identity and explicit phase marker validate. Maintained Python entry points use a process-wide audit hook; the semantic read helper rejects unguarded invocation. Network and unapproved imports/processes are blocked in guarded tasks.

This is a trusted-workflow boundary, not an operating-system sandbox against hostile code. Fresh reviewer sessions share a filesystem/model family. The parent retains historical aggregate knowledge but does not author review labels. Bootstrap method-source reads and git/security operations were separately restricted to source, current artifacts and metadata. No independent-test claim follows from this isolation.

## Pre/post test classification
The machine-readable test-phase manifest defaults unclassified historical gates to POST_GOLD_ONLY. Pre-freeze checks comprise 33 generic semantic/heading tests and 12 synthetic guard checks, plus exact population/hard-audit/workspace assertions. R06 and all broad historical/production gates are deferred.

## Structural-heading policy
Structural line/table labels and non-propositional colon prefixes cannot supply semantic cues to body content. Finite propositions, explicit questions and CLM assertions remain content even in heading positions. Exact source offsets, negation and modality remain intact. No packet-specific synonyms or new ontology attributes are admitted. A detected epistemic/absence proposition outside the finite ontology raises an explicit unresolved error; it is not silently recast as a heading.

V2 distinctions remain: conflict versus epistemic uncertainty; absence versus unknown occurrence; recording/event/submission/authorization roles; order versus content; first/next and previous/later; source uncertainty versus review ambiguity; exact support and owned references versus flexible canonical wording.

## Population and review
Frozen population: 64 substantive producers, 32 TRAIN and 32 DEV, eight domains and eight attainable templates. No reserve or evaluation data. Prospective hard audit: 16, eight per split, covering headings, absence, temporal roles, order/content, contradiction, multiple claims and evidence selection. Fresh A/B/C each receive all 64 packets, generic policy/schema/instructions and bindings. Their judgments are not human review.

## Fresh review, consensus and adjudication
192/192 primaries valid; zero invalid, zero repairs. All 64 packets have 3/3 material agreement; 2/3=0; no majority=0; valid triple rate=100%. All 16 hard adjudications validate, with zero corrections and zero unresolved ambiguity. Each fresh reviewer and adjudicator attested to workspace-only access and one substantive judgment per assignment. These attestations are preserved with the labels.

## Clean label freeze and first target access
Label freeze: `aad0ec046d7f507d522bf094771b56ea16fd94c9`. Its manifest binds 330 files. The guard verified a commit object containing the exact manifest and all current freeze hashes before creating the explicit post-gold marker. Pre-gold authored-target reads: **0**. First target-open proof and final access audit are recorded with post-freeze artifacts.

## Post-freeze diagnostic comparison
| Dimension | Agreement |
|---|---:|
| Claims | 64/64 |
| Typed gaps | 58/64 |
| Typed uncertainty | 64/64 |
| Evidence | 64/64 |
| Refusal | 64/64 |

58 cases agree semantically. Six are source-supported legacy under-specification, established by a strict legacy semantic subset plus valid primaries/final labels and exact grounded extra atoms under the frozen policy. No real material disputes, machine semantic errors or unresolved diagnostics remain. Canonical prose byte equality is not an eligibility criterion. No V3 semantic rule or label changed after comparison.

## Producer candidate coverage
| Measure | TRAIN | DEV |
|---|---:|---:|
| Eligible candidates | 32 | 32 |
| Domains | 8 | 8 |
| Templates | 4 | 4 |
| Document families | 20 | 20 |
| Gap cases | 32 | 32 |
| Source-uncertainty cases | 26 | 26 |
| Multiple-claim cases | 29 | 29 |
| Evidence-selection cases | 29 | 29 |

The combined producer population covers all eight attainable templates and 40 document families. Every canonical target validates. All 32 substantive TRAIN examples qualify, meeting the unchanged TRAIN>=32 and DEV>=16 thresholds.

## Deferred regressions and remaining completion
Production deterministic gate: 85 checks passed, four effect proofs, one existing optional skip. Initial post-only harness failures were preserved: Windows lacks os.O_ACCMODE, and asyncio requires a loopback wakeup pair. The separate post runner supplies the equivalent mask and creates that stdlib resource before installing the same target/network boundary. It does not alter the frozen guard, semantics, labels or production validators. The guard remains active for every target read and subsequent socket creation.

The post-only R06 checker initially referenced the older first-experiment selection (153 non-TRAIN rows), rather than the maintained cohort-v2 selection. A separate post adapter corrects this reference; the original frozen method source is preserved. The corrected selection exactly equals the original frozen R06 population. This was validation wiring, not a semantic-method or label change.

The full cohort gate was stopped because human-ingestion fixtures repeatedly rehashed the same historical catalogs. The completed replacement combines all catalog hashes with 24 relevant selection/access/binding/blank-export checks, three effect proofs and four historical rejection reproductions. Seven original cohort tests were not rerun to completion; the execution evidence lists them. Stopped runs are not counted as passes.

Validation totals: **45 PRE_GOLD_SAFE tests** (33 semantic/heading, 12 guard); **110 POST_GOLD_ONLY tests/checks** (24 cohort, 85 production, one R06 TRAIN-exclusion effect), plus four production effect proofs, four historical rejection reproductions and 3,508 historical catalog hash comparisons. No source rules or labels were patched from post-gold diagnostics.

## Preserved auditor pool and balanced readiness
Auditor labels remain **46 TRAIN / 24 DEV**, with their entire historical release verified. The role-separated manifest references exact source hashes, explicit role filters, allowed IDs and selected-row hashes. It does not merge producer and auditor target formats or issue an execution/training manifest.

Combined machine-curated pool: **78 TRAIN / 56 DEV**. Producer coverage satisfies every unchanged criterion, including all 32 substantive TRAIN examples, DEV>=16, eight domains, all eight attainable templates, gaps and uncertainty in both splits, multiple claims, evidence selection, canonical contracts, zero evaluation leakage and clean chronology.

`PRODUCER_TUNING_COVERAGE_READY`

`BALANCED_AGENT_ADJUDICATED_TUNING_EXPERIMENT_READY`

`FIRST_TUNING_EXPERIMENT_HUMAN_REVIEW_STATUS=PENDING`

Human first reviews, second reviews and adjudications remain **0 / 0 / 0**.

## R06 and acceptance preservation
R06 retains exactly **114 non-TRAIN examples: 40 DEV, 37 TEST, 37 public-adversarial**. Its population does not expand with the new producer DEV candidates. Neutralising TRAIN exclusion changes the synthetic invariant; restoring it passes. The 27 registered quantitative criteria, derived R07 and six catastrophic zero limits are unchanged. This verifies preservation, not new model-performance or checkpoint acceptance scores.

## History, limitations and next action
Machine-review V1/V2, producer amendments V1/V2, all quarantined V2 labels, prior freezes/governance failure, auditor pool, benchmark/task releases, first-experiment registration, production validators and historical A/B evidence remain unchanged. Catalog counts: machine V1 961, machine V2 692, producer V1 406, producer V2 338, A/B 106, first experiment 502, current cohort 503.

Clean chronology and producer label-curation coverage are established; balanced machine-adjudicated tuning feasibility is ready. Fresh same-family agent sessions and procedural workspace isolation do not constitute human validation, cross-model independence, protected-test generalization, production readiness or model acceptance. Unsupported language beyond the finite ontology remains outside this result.

Next action: a separately authorized tuning experiment may be proposed; this run grants no training permission. Human review remains pending. No V4 was created.

No cloud, paid inference API, Shimmer model execution, training, LoRA/SFT, model-ID/revision/quantization change, full pipeline, multi-round or push occurred. All commits are local.

## Evidence and execution
- Method: `benchmark/producer_coverage_amendment_v3/PRE_REVIEW_METHOD_FREEZE.json`
- Labels: `blind_evidence/PRE_GOLD_FREEZE.json`
- First access: `post_freeze/FIRST_TARGET_ACCESS.json`
- Results: `post_freeze/readiness.json`, `legacy_summary.json`, `historical_integrity.json`
- Role-separated pool: `post_freeze/balanced_role_separated_candidates_v3.json`
- Validation: `post_freeze/validation_evidence/` retains executed adapters, catalog proofs, logs, stopped/failed harness evidence and selected-test lists. Adapters were executed from `output/producer_coverage_amendment_v3`; the archived copies preserve those original path assumptions. Use the corrected post adapter, not the retained wrong-cohort post-check entry, for this completed run.


`FIRST_TUNING_EXPERIMENT_HUMAN_REVIEW_STATUS=PENDING`

No cloud, paid API, Shimmer model execution, training, LoRA/SFT, model change, full pipeline, multi-round or push is authorized in this run.

First successful authored-target open: `2026-09-15T19:30:04.419558+00:00` (`benchmark/task_semantics/seed.json`), after label commit at `2026-09-15T19:29:36Z`. Verified manifest SHA256: `d750fdff3883102292e4f31e8cd0c86612a743571db56d02663ac476b4439fa9`.

Final artifact security scan: **935 files, zero credential findings**. No operator documents or durable private state were used.
