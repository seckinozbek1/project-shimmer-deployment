{
  "compact_local": "Copied prior deterministic fixtures; validation.json and token_budget.json were regenerated by the pre-provision gates. Files prefixed seed_ are the prior readiness context, not the current artifact manifest.",
  "manifest": "ARTIFACT_HASHES.json covers all retained files except itself.",
  "raw_outputs": "evidence/probe/probe.json retains unmodified raw and parser outputs. semantic_adjudication.json records independent raw observations without accepting repaired output.",
  "source_archive": "Exact reviewed 172-member source archive retained; credentials/operator state excluded."
}
