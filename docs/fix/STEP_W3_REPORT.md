# STEP W3 REPORT: firing, and the fourth visible state

## Skipped, per this step's own opening line

"Follows from W2, not a separate mechanism. An agent with no conventions assigned does not
run." W2 (see `docs/fix/STEP_W2_REPORT.md`) stopped at its own premise check: no per-agent
convention assignment exists, and building one would require either a hardcoded subject
mapping (forbidden, S5) or a new shared vocabulary neither agent contracts nor conventions
declare today, a real design decision left to the operator, not decided in this chain's own
text.

With no assignment mechanism, "an agent with no conventions assigned" is not a state that can
occur: every agent still receives every convention, exactly as before tonight's chain began.
Building a fourth visible console state for a firing decision that never happens would be
dormant scaffolding, the shape this project's own standing discipline explicitly rules out
("No dormant-but-claimed-built scaffold is acceptable: every part... must have a live duty or
an executed gate path"). A gate check proving "an agent with no assigned rules does not run"
would have nothing real to neutralise and restore against, since no code path decides that
today.

Nothing built. Nothing changed. Gate unchanged: `PASS=193`, same as W2.

The three visible states this chain's own earlier work already built and proved (an
amendment, a refused finding, a failed contract, committed tonight before this chain started)
remain exactly as they are. W8 (the console step) will state this same skip when it reaches
the point of asking whether a fourth state exists to surface.

---

STEP W3 COMPLETE (skipped, per its own dependency on W2, which stopped)
