"""External corpus ingestion package (corpus-boundary handoff).

Holds the ingestion-contract validator (Item 1) and the promotion-exclusion helper
(M1). Integration logic lives here, not inline in the Shimmer pipeline.
"""
