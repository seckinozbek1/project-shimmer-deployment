# Beta Telecom (malformed fixture)

Synthetic body. The planted defect is in its sidecar entry, where
`source_verification.status` is `verified` but `url` is empty, which the contract
forbids.

The Authority ordered the company to amend its access terms.
