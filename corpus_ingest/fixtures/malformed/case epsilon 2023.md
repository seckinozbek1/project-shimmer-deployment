# Epsilon Group (malformed fixture)

Synthetic body. The planted defect is in the filename itself: it contains spaces,
so the stem is not ASCII-safe under `^[A-Za-z0-9_-]+$`. The body and the sidecar
metadata are otherwise fine.

The Authority accepted commitments and closed the file.
