# Fresh blinded producer review

Read POLICY.md and SCHEMA.json, then each assigned packet. You are an independent fresh primary reviewer. Do not read outside this assigned workspace. No network, model tools, gold, V1/V2 outputs, admin mappings, other reviewers or parent conversation. No delegation. Packet IDs are opaque; no splits or expected answers are supplied.

For each packet produce exactly one JSON in reviews/<packet_id>.json, once. Do not overwrite or semantically retry. You may write a local serializer after individually examining all packet source content; the serializer must encode your own per-packet judgments rather than infer answers from filenames or another reviewer. No model/source-to-answer heuristic can replace your independent semantic reading. All assigned packets must be read.

Outer fields: protocol="producer-coverage-amendment-v1", policy="producer-semantic-policy-v1", packet_id, input_sha256 (SHA256 of canonical JSON packet input: sort_keys=True,separators=(",",":"),ensure_ascii=False), slot="A"/"B"/"C", provenance="machine_agent_primary", reviewed_at ISO UTC, label, rationale. Copy packet binding exactly. label has status="examined" or "refused", items, source_uncertainty_present boolean, review_ambiguity boolean. Each examined owned span gets exactly one item in input order: span, claims array of all explicit CLM ids, refs array of all owned REF ids, gap_atoms array, uncertainty_atoms array. Refusal has items=[] and source_uncertainty_present=false. No canonical wire response required: Python renders your typed label.

Each atom has kind,category,subject,attribute,relation,scope,unit,span,refs,origin,support. Follow the frozen policy. Keep support a concise EXACT owned source substring proving the annotation. All atom refs equal the owned item's refs, even if the qualifying sentence has none. source_uncertainty_present equals presence of uncertainty atoms. Use review_ambiguity only for genuinely ambiguous annotation; source uncertainty alone is not ambiguity.

Read every owned source span and context boundary. Keep every explicit question: an answered layout question uses category explicit_question with its semantic subject/attribute/order relation; unanswered/missing information uses missing_information. Do not add uncertainty merely for conflicting claims. Preserve actual explicit uncertain interpretation/state. Do not judge or extract context-only content. Typed target material, not arbitrary support/rationale wording, determines consensus.

Allowed attributes: reporting_date, recording_date, observation_date, effective_time, recorder, interpretation, ratification_status, entry_content, entry_order, quantity, identity, state. Subject owned_record unless explicit entry ordinal, amendment, or entity referent. See POLICY.md. For generic record date use recording_date. Relation null except explicit first/next/before/after. Gap kind=gap; uncertainty kind=uncertainty category=unknown. Unit null unless explicitly material. Scope owned_record normally, origin source_explicit.

On completion report count, unresolved ambiguity count and isolation attestation. Do not access any other directory to validate your outputs.
